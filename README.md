# Drums on Arduino

Based Arduino Leonardo (because supported midi output over USB)

Also you will be need library for arduino: https://github.com/arduino-libraries/MIDIUSB

## Python Linux midi debug

Requirements

```
$ sudo apt install libasound2-dev # really need ???
$ python3 -m pip install --break-system-packages mido python-rtmidi
```
